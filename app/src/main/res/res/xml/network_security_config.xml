<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production-grade network security configuration -->
    
    <!-- Base configuration for RELEASE builds -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust only system Certificate Authorities in production -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug configuration (allows user-installed CAs for development) -->
    <debug-overrides>
        <trust-anchors>
            <!-- In debug builds, trust user-installed CAs (e.g., Charles Proxy, Fiddler) -->
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!-- Domain-specific configuration (if needed for Firebase or other services) -->
    <domain-config>
        <domain includeSubdomains="true">firebaseio.com</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">google.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
